{
  "hooks": {
    "pre-commit": [
      "eslint --ext .ts,.tsx src/",
      "tsc --noEmit",
      "git-secrets --scan"
    ],
    "pre-push": [
      "bun run test",
      "bun run build"
    ]
  }
}
